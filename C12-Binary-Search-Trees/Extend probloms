Exercises 12-1
带有相同关键字的二叉搜索树问题。
a.将带有相同关键字的数据插入一个初始化为哦空的二叉搜索树中，其渐进性能？
解决办法：
在插入的第5和第11行，添加对插入关键字相等的判断策略。
b.对x节点添加一个bool值，根据改值判断分配x.left还是x.right。该bool值交替置为false和true。
c.对于x相同的节点内部添加一个列表，存储该添加的值。
d.随分配到x.left或x.right，分析其最坏性能，和期望运行时间。
Answer
a:Θ(n^2)
b:Θ(nlgn)
c:如果内部列表插入方式为头查法Θ(nlgn),如果遍历尾插Θ(n^2)
d:最坏情况为Θ(n^2)
  期望分析，根据随机构建二叉搜索树的期望时间分析：
  E(Yn) <= 2/n * ∑E[max(Yi-1,Yn-i)]
  在max(Yi-1,Yn-i)选取时，我们是随机选择其中一个则同样有
  E(Yn) <= 2/n * ∑[E(Yi-1)+E(Yn-i)]
  同理，期望时间
  O(nlogn)
